@model ForgotPasswordViewModel
@{
	Layout = null;
	ViewBag.Title = "Esqueci minha senha";
}

<!DOCTYPE html>
<html>
<head>
	<meta charset="UTF-8">
	<title>Manager | @ViewBag.Title</title>
	<meta content='width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no' name='viewport'>
	<!-- Main Styles -->
	@Styles.Render("~/styles/main")
	<!-- HTML5 Shim and Respond.js IE8 support of HTML5 elements and media queries -->
	<!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
	<!--[if lt IE 9]>
		@Scripts.Render("~/scripts/ie-hack")
	<![endif]-->
</head>
<body class="login-page">
	<div class="login-box">
		<div class="login-logo">
			<a href="@Url.Action("Index", "Home")"><b>M</b>ANAGER</a>
		</div><!-- /.login-logo -->
		<div class="login-box-body">
			<p class="login-box-msg">Informe o endereço de email cadastrado em sua conta</p>
			@using (Html.BeginForm("ForgotPassword", "Account", new { returnUrl = ViewBag.ReturnUrl }, FormMethod.Post))
			{
				@*@Html.ValidationSummary(ModelStateType.Error, new { @class = "alert-danger" })*@
				<div class="form-group has-feedback">
					@Html.TextBoxFor(_ => _.MailAddress, new { type = "email", @class = "form-control", placeholder = "Email" })
					<span class="glyphicon glyphicon-envelope form-control-feedback"></span>
				</div>
				<div class="row">
					<div class="col-xs-12">
						<button type="submit" class="btn btn-primary btn-block btn-flat">Enviar</button>
					</div><!-- /.col -->
				</div>
			}

			<a href="@Url.Action("Login", "Account", new { returnUrl = ViewBag.ReturnUrl })">Voltar para o login</a><br>

		</div><!-- /.login-box-body -->
	</div><!-- /.login-box -->
	<!-- Main Scripts -->
	@Scripts.Render("~/scripts/main")
	<script>
		$(function () {
			$('input').iCheck({
				checkboxClass: 'icheckbox_square-blue',
				radioClass: 'iradio_square-blue',
				increaseArea: '20%' // optional
			});
		});
	</script>
</body>
</html>